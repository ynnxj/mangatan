declare global {
  namespace App {
    // interface Error {}
    // interface Locals {}
    interface PageData {
      news?: Post[];
    }
    // interface PageState {}
    // interface Platform {}
  }
}

export {};
